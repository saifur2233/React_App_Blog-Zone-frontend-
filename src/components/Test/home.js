import React from 'react'

const home = () => {
    // const truncateString = (str, num) => {
    //     if(str.length > num) {
    //         return str.slice(0, num) + "..."
    //     } else{
    //         return str
    //     }
    // }
  return (
    <div>home</div>
  )
}

export default home